[project]
name = "mentor-research-project"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
description = "Research training project for empirical accounting and corporate finance"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
