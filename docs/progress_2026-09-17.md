# Research-Policy_Innovation_Demo 阶段进展

日期：2026-09-17

## 一、当前阶段

当前项目已从原始数据质量审计进入正式数据清洗阶段。

本项目当前使用模拟训练数据，主要用于练习财务会计、公司金融与企业创新研究中常见的数据工程、面板构建和实证分析流程。

## 二、已完成工作

### 1. 财务面板第一轮清洗

已建立：

`notebooks/02_data_cleaning.ipynb`

当前已完成：

- 股票代码标准化；
- 年份标准化；
- firm-year 主键重复检查；
- 完全重复与冲突重复分类；
- 完全重复记录安全去重；
- 财务数值字段初步格式审计。

原始财务数据共有 260 条记录。

完成完全重复去重后，当前工作数据共有 250 条记录。

### 2. firm-year 重复问题

标准化主键后共发现：

- 20 个重复 firm-year 组；
- 其中 10 个为完全重复组；
- 10 个为冲突重复组。

已删除完全重复组中的多余副本。

冲突重复组全部保留。

进一步检查发现，当前 10 个冲突组均只在 `net_profit` 字段存在不同取值，因此暂不进行机械删除或取平均处理，等待后续明确数据来源和处理规则。

### 3. 财务数值字段审计

已识别：

- 金额字段中的千位逗号格式；
- `"-"` 形式伪缺失值；
- pandas 原生缺失值；
- ROE 百分号字符串；
- ROE 中需要进一步判断的异常值或尺度问题。

目前尚未对 ROE 进行机械转换，以避免混淆百分数和小数口径。

### 4. Stata 环境

Stata/MP 18 已完成安装、更新和基本运行验证。

当前已经验证：

- GUI 正常使用；
- `.do` 文件可运行；
- 命令行批处理可用；
- VS Code 可作为后续 Stata 编辑与运行环境。

Stata 后续作为研究工具之一使用，而不是强制替代 Python。

当前工具分工原则为：

- Python：数据清洗、数据工程、文本处理、匹配与自动化；
- Stata：在面板回归、固定效应、DID、稳健性检验等传统实证任务中按需要优先考虑。

已建立：

`stata/00_environment_check.do`

用于验证基础 Stata 脚本运行环境。

## 三、当前科研工作原则

当前继续坚持：

- raw 数据不直接修改；
- 先审计，后制定清洗规则；
- 对冲突数据不进行无依据删除；
- 清洗前后进行数量和主键验证；
- Python 与 Stata 按任务适配，而不是固定使用单一工具；
- 代码、规则与研究过程保持可复现。

## 四、下一阶段

下一阶段将继续：

- 清洗金额和人数等数值字段；
- 统一伪缺失值；
- 进一步研究 ROE 的数据口径和异常值；
- 对 `net_profit` 冲突记录建立明确处理方案；
- 输出标准化 processed 财务数据；
- 清洗并匹配公司基本信息与专利数据；
- 构建完整 firm-year 研究面板。

完成基础面板后，再逐步进入政策文本处理、变量构造和 Stata 实证分析。